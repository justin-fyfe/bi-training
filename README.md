# Training Applet

This applet contains training materials for the BI layer of the SanteDB system. You should have your iCDR or dCDR load this applet via the configuration file:

```
<section xsi:type="sdk:DebugAppletConfigurationSection">
    <!-- Truncated -->
    <sdk:add>..\..\..\training</sdk:add>
</section>
```